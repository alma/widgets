/// <reference types="react" />
declare type Props = {
    className?: string;
    color?: string;
    monochrome?: boolean;
    underlineColor?: string;
};
declare function LogoIcon({ className, color, monochrome, underlineColor, }: Props): JSX.Element;
export default LogoIcon;
