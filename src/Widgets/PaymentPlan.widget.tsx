import React from 'react'
type Props = {
  purchaseAmount: number
}

const PaymentPlanWidget: React.FC<Props> = ({ purchaseAmount }) => {
  return <div>hregdq</div>
}
export default PaymentPlanWidget
