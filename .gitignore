node_modules
coverage
.nyc_output
.DS_Store
*.log
.vscode
.idea
compiled
.awcache
.rpt2_cache
docs
stats.html
dist/
raw/
clean-package/

# Local playground (see .claude/skills/playground-builder) — scratch, per-session, never committed.
# Anchored to the repo root (leading slash): an unanchored `playground/` also matches
# .claude/skills/playground-builder/assets/playground/, which must stay tracked.
/playground/
/vite.playground.config.ts
